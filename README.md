This is allure playtests for pytest.

To run you need allure installed:

    pip install allure-pytes

To run test:

        pytest --alluredir=%allure_result_folder% ./<folder>

        allure serve %allure_result_folder%

